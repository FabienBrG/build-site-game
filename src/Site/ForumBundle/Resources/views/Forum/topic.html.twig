{% extends "::base.html.twig" %}

{% block body %}
    {% for topic in topic %}
    <div class="container">
        <div class="jumbotron"style="margin-top:30px;padding-top:10px;">
          <div class="row">
            <div id="titre-post">
              <h2>{{topic.titre}}</h2>
            </div>
          </div>
          <br/>
          {% for comment in topic.listComment %}
            <div class="row">
              <div id="info-member" class="col-lg-3">
                <div class="col-lg-6"></div>
                <a href="#" class="thumbnail" >
                  <img src="{{ asset('images/avatar/'~comment.getFkAuteur.id~'.jpg')}}" alt="...">
                </a>
                @{{comment.getFkAuteur.pseudo}}
              </div>
              <div id="post" class="col-lg-6 text-justify">
                {{comment.contenu}}
              </div>
              <div id="after-post" class="col-lg-3">
                {{comment.datePublication|date('d-m-Y H:i:s')}}
              </div>
            </div>
            <br/>
           {% endfor %}
			<div class="row">
				{{form(form)}}
			</div>
        </div>
      </div>
    {% endfor %}
{% endblock %}
{% block javascripts %}
    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.2/jquery.min.js"></script>
    <!-- Include all compiled plugins (below), or include individual files as needed -->
    <script src="{{ asset('js/bootstrap.min.js') }}"></script>
{% endblock %}